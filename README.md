A simple tool to show how maven treats and compares versions.


## Usage

build the project with `mvn package`

run the jar with `java -jar target/versiontest-0.0.1-SNAPSHOT.jar versionString version2String`



